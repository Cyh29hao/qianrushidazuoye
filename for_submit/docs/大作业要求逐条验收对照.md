# 大作业要求逐条验收对照

更新时间：2026-06-16

主依据：`docs/大作业要求/大作业题目-学生版_V1.2.pdf`。该 PDF 为扫描/图片式文件，普通文本抽取只有换页符；本文中的“老师原文要求”为按渲染页面人工转写/摘录，保留原章节顺序和核心措辞。`FAQ_常见问题解析_V1.0(1).pdf`、`MCU与PC端开发要求_V1.0(1).pdf` 仅作为补充自查，不推翻正式题目。

当前主工程：`D:\桌面\大二下\大二下 嵌入式系统与接口技术\ARM\真正的最新版`

## 最新验收结论摘要（2026-06-16）

当前 v2.2 主线已通过老师新发 `testcmdV2.1` 串口命令测试子集的 COM5 真机评分：`OK 92`、`ERROR 8`、`TIMEOUT 0`、评分 `10.0/10.0`。8 个 `ERROR` 来自脚本故意构造的非法命令，用于验证协议拒绝能力，不属于扣分问题。

已经完成并验证的关键点：

- Keil5 重新编译最新 MCU 工程：`0 Error(s), 0 Warning(s)`；烧录后下载校验 `Verify OK`。
- MCU 协议支持老师分组参数语法：`*SET:DATE YEAR MONTH DATE 2026 06 15`、`*SET:TIME HOUR MIN SEC 12 30 45`、`*SET:ALARM HOUR MIN SEC 12 31 00`；同时保留旧 PC 交替参数格式兼容。
- 缩写策略已收紧：合法缩写保留，`MONT`、`MI`、`FOR` 等不应接受的歧义缩写会返回 `ERROR PARAM/SYNTAX`。
- 串口稳定性已针对实测问题加固：启动栈扩大到 2048 B、主循环持续回到 `UART_Poll()`、`*EVT:DISP` 输出做可打印字符保护，避免乱码帧和 TIMEOUT。
- PC 上位机、协议构造器、扩展测试脚本和新建严格回归脚本均已同步老师语法。

仍需最终人工制作的内容是简介 PDF、演示视频和提交 zip；不是代码主功能缺口。

## 最新验收结论摘要（2026-06-11）

当前 v2.2 主线已经进入“可按要求逐项演示”的状态。PC 上位机、数字孪生、串口协议、NTP/天气、自动昼夜、板载闹钟、PC 多日程提醒、调试协议台、自动测试、README 说明书和 release 打包均已完成主体功能。

已经可以点头的项目：

- S800 基础功能：开机画面、时间/日期/星期/年份显示、DISP 循环、FORMAT/SPEED、闹钟、LED、蜂鸣、USER1/USER2、串口协议主命令均有实现。
- PC 协同功能：串口连接、TX/RX 日志、数字孪生、虚拟按键、状态同步、协议测试台、自动测试均可演示。
- 扩展功能：NTP 对时、天气短显、全球城市/时区、自动昼夜、多日程提醒、铃声类型、语音空文本规则、恢复出厂设置、打包版 exe 均已实现。
- 自动测试：COM5 全面联合测试已通过，包含 USER2、跑马灯下划线、DISP/SPEED/FORMAT/EXT、ERROR 类型和高并发按键鲁棒性；测试支持中止、失败即停、恢复测试前设置。
- release：`build_release/SmartClockHost-v2.2/SmartClockHost.exe` 和 zip 已重新生成，release 文件夹/zip 内包含最新 MCU 工程副本，便于 Keil5 编译烧录。
- 最新收口项：PC 日程提醒触发时会把板端显示消息、命名铃声和短蜂鸣确认一起下发，并在日志中记录 `INFO 日程提醒铃声: ...`；底部状态栏左侧已统一为 `智能联网时钟系统`，feature 文案去重；打包版 exe 已重新嵌入并验证项目图标。
- E4 数据可视化：主页新增“卡片看板 / Matplotlib 图表”切换，图表支持今日时间轴、提醒分布、系统状态、24 小时日志统计四类筛选，并随白天/黑夜主题重绘。
- 最新体验复测：Matplotlib 已改为按需懒加载，串口扫描已后台化，不使用串口时 `DISP/USER2/MSG` 等离线操作可在几十毫秒内即时反馈；COM5 全面联合测试再次通过，覆盖 USER2、下划线跑马灯、DISP/SPEED/FORMAT/EXT、高并发按键和 ERROR 四类。

仍需最终人工确认的项目：

- MCU 源码已更新并完成 Keil5 编译、COM5 烧录与老师 testcmdV2.1 复测；正式演示前只建议做一次快速复核，不再是未完成项。
- 简介 PDF 和演示视频仍需要按最终要求制作；本文件、README 和 `docs/自主答辩准备初稿.md` 可作为准备素材。
- Matplotlib 是 PC 端新增依赖；源码版需要重新 `pip install -r pc_host/requirements.txt`，打包版 exe 已随包携带依赖。
- 打包 exe 临时 profile 冷启动未闪退，但 10 秒采样点仍可能短暂 `Responding=False`，约 15 秒恢复；演示时建议提前打开程序，避免把 PyInstaller/Qt 首次加载时间录进正式视频。

## 1. 作业概述

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | “利用 S800 板与 PC 上位机软件，共同搭建一台能本地独立运行、又可与 PC 双向协同的数字时钟系统。”S800 离线完成时钟、闹钟、按键设置；PC 通过 USB 虚拟串口实现远程控制、状态监控、镜像显示；PC 提供 S800 1:1 数字孪生面板；协议统一、容错可靠。 |
| 当前实现方式 | MCU 主逻辑在 `mcu/src/main.c`，PC 上位机在 `pc_host/app.py`，串口协议解析在 `pc_host/protocol.py`。PC 右侧提供 8x7SEG + 8 LED + 8 按键 + USER1/USER2 数字孪生。当前主版本为 v2.2。 |
| 实际效果 | 板端可脱离 PC 走时、显示、按键设置、闹钟；PC 可连接 COM 口控制板端，也可选择“不使用串口”进入离线数字孪生演示。 |
| 验收/演示方法 | 先断开 PC 只操作板子，再连接 COM5，观察 `*EVT:DISP`、`*EVT:LED`、按键同步和 PC 虚拟按键下发。 |
| 是否满足 | 满足；最新 MCU 已 Keil5 编译烧录并通过老师 testcmdV2.1 真机评分。 |
| 创新/加分点 | 网络对时、天气、全球时区、自动昼夜、PC/板端双向昼夜控制、主题同步、日程提醒、语音空文本规则、快速/全面两套自动测试、高并发防卡死、GitHub 版本管理、打包版 PC 程序和用户友好界面。 |

## 2. 硬件资源与系统架构

### 2.1 硬件资源

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | USB 虚拟串口用于 PC 通信；8 位动态七段数码管用于时钟显示、滚动消息、扩展短显、开机画面；8 位按键 + USER1/USER2 用于本地设置和扩展触发；8 位 LED 用于心跳/状态/扩展信息；蜂鸣器用于闹钟、远程触发、操作反馈。 |
| 当前实现方式 | MCU 使用 UART0、I2C 数码管、I2C 键盘、PCA9557 LED、GPIO 蜂鸣器；PC 用 pyserial 通信。 |
| 实际效果 | 数码管、LED、蜂鸣、按键均有代码路径；PC 数字孪生能映射显示和按键。 |
| 验收/演示方法 | 开机看全亮和学号；按键切换显示/速度/格式；PC 点击虚拟按键；触发蜂鸣测试。 |
| 是否满足 | 满足，硬件最终以 Keil 编译和实板验证为准。 |

### 2.2 系统架构框图

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | S800 与 PC 双向通信；PC 侧包含控制面板、数字孪生镜像、日志、扩展模块；虚拟按键通过 `*SET:KEY` 下行，构成双向同步。 |
| 当前实现方式 | PC 左侧为主页/系统设置/闹钟与日程管理/调试与测试；右侧固定数字孪生和日志；`send_virtual_key()` 下发 `*SET:KEY <NAME>`。 |
| 实际效果 | 真实板卡事件可同步到 PC；PC 点击虚拟键可模拟物理键；本地模式也能生成镜像。 |
| 验收/演示方法 | 点击 PC 的 FORMAT、DISP、USER1 等虚拟键，观察板端或本地镜像变化。 |
| 是否满足 | 满足。 |

## 3. S800 板端功能要求

### 3.1 开机画面

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 8 位数码管 + 8 位 LED 全亮到全灭闪烁不少于 1 次；显示学号后 8 位并闪烁；显示姓名拼音并闪烁；显示版本号至少 1 秒后进入正常时钟。 |
| 当前实现方式 | `BOOT_ALL_ON/OFF/ID/NAME/VERSION` 状态机；全亮帧 `88888888 FF`；学号后八位 `31910102`；姓名拼音 `CHENYH`；版本 `V2.2`。 |
| 实际效果 | MCU 开机动画与 PC READY 后镜像播放对齐；真实 `*EVT:DISP` 到达后 PC 继续跟随实物。 |
| 验收/演示方法 | RESET 后录像：全亮/全灭、学号、拼音、版本、进入时钟。 |
| 是否满足 | 满足；需重新烧录后现场验证。 |

### 3.2 时钟与日期显示

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 默认显示时间 `HH.MM.SS`；按键可切换日期 `YY.MM.DD` 或 `YYYY.MMDD`；闰年、月末进位、23:59:59 到 00:00:00 正确；走时基准来自 1 ms 系统时基。 |
| 当前实现方式 | `BuildDottedTime()`、`BuildDottedDateShort()`、`BuildDottedDateYear()`；`DaysInMonth()`、`IsLeapYear()`；SysTick 1 ms，`Tick1000ms()` 推进时间。 |
| 实际效果 | 时间、短日期、年日期均可显示；本轮新增 DISP 周几页。 |
| 验收/演示方法 | 设置 2028-02-29、2026-06-30、23:59:58，观察进位；按 DISP 循环查看页面。 |
| 是否满足 | 满足；长时间误差需实板测。 |

### 3.3 流水显示

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 显示内容多于 8 位时按固定速率流水；方向由 `*SET:FORMAT LEFT/RIGHT` 或 FORMAT 键切换；速度 2 级可调；小数点随方向跟随。 |
| 当前实现方式 | `VisibleTextToCells()` 将点号转为物理 dp 位；`FORMAT RIGHT` 逆序并调整 startIndex；SPEED 键切换快慢；消息和周几页采用有限端点滚动。 |
| 实际效果 | 时间 `12.30.45` 可按物理位上报 `12_30_45 24`，RIGHT 为 `54_03_21 24`；长消息和 `WEDNESDAY` 可滚动到端点。 |
| 验收/演示方法 | 下发 `*SET:MSG Hello Clock`、切换 FORMAT/SPEED，观察 MCU 和 PC 镜像。 |
| 是否满足 | 满足。 |

### 3.4 闹钟功能

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 时分秒相等触发；蜂鸣器节奏式响铃，持续不超过 10 秒自动停止；响铃中按 FUNC 立即停止；LED 指示闹钟使能/响铃状态。 |
| 当前实现方式 | `g_alarm`、`StartAlarmRing()`、`ServiceBuzzer()`；FUNC 在响铃中优先停止；LED_ALARM 表示使能/响铃。 |
| 实际效果 | 板载单次闹钟可由板端或 PC 设置/关闭；PC 单次闹钟 UI 显示当前状态和触发时间。 |
| 验收/演示方法 | 设置未来 1 分钟内闹钟，观察蜂鸣和 LED；按 FUNC 停止；等待自动停止。 |
| 是否满足 | 满足，需实板蜂鸣验证。 |

### 3.5 按键设置（编辑状态机）

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | FUNC 短按循环“日期 -> 时间 -> 闹钟 -> 退出”；SHIFT 高亮字段；ADD 加 1 并处理进位/钳制；SAVE 或 FUNC 长按保存退出；5 秒无操作自动退出且不保存；1 秒内连续按 3 次以上不丢键。 |
| 当前实现方式 | `EDIT_DATE/EDIT_TIME/EDIT_ALARM` 状态机；`AdvanceEditField()`、`IncrementEditField()`、`ExitEditMode()`；长按阈值约 800 ms；5 秒超时。 |
| 实际效果 | 本地可编辑日期、时间、闹钟；PC 日志收到 `*EVT:EDIT` 保存事件。 |
| 验收/演示方法 | 连续按 FUNC、SHIFT、ADD、SAVE；无操作等待 5 秒；观察是否保存。 |
| 是否满足 | 满足，按键硬件防抖需实测。 |

### 3.6 LED 辅助指示

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 8 位 LED 全部使用；至少包含系统心跳、闹钟状态、编辑模式、串口收发活动四类指示。扩展天气/NTP 可用后 4 位，但不能替代基础指示。 |
| 当前实现方式 | LED0 心跳、LED1 闹钟、LED2 编辑、LED3/LED4 串口 RX/TX、LED5 NIGHT、LED6 FORMAT RIGHT、LED7 手动/扩展；天气短显会临时覆盖 LED。 |
| 实际效果 | PC 数字孪生同步 LED；NIGHT 模式只保留心跳。 |
| 验收/演示方法 | 待机、编辑、串口收发、闹钟、天气短显分别观察 LED 和 PC 镜像。 |
| 是否满足 | 满足。 |

### 3.7 按键完整映射

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | FUNC、SHIFT、ADD、SAVE、DISP、SPEED、FORMAT、EXT、USER1、USER2 均有定义；DISP 主显示切换时间/日期/年份；SPEED 两级速度；FORMAT 左右流水；USER1 请求 PC 对时；USER2 短显天气。 |
| 当前实现方式 | `kKeyNames` 和 `HandleKeyPress()` 覆盖 10 项；本轮 DISP 顺序为 TIME -> DATE -> WEEKDAY -> YEAR；USER1 短按上报 PC 对时，长按切换 DAY/NIGHT；USER2 显示天气。 |
| 实际效果 | 板端与 PC 虚拟按键语义一致；新增周几页位于日期页后。 |
| 验收/演示方法 | 逐个按键操作，同时看串口日志和数字孪生。 |
| 是否满足 | 满足；新增周几为本轮增强项。 |

## 4. PC 上位机功能要求

### 4.1 核心功能 P1-P5

| 编号 | 老师原文要求 | 当前实现方式 | 演示方法 | 是否满足 |
| --- | --- | --- | --- | --- |
| P1 | 串口管理与状态栏：自动扫描 COM、连接/断开、后台读、心跳与延迟显示；状态栏显示连接、FORMAT、MODE、ALARM。 | `refresh_ports()`、`connect_and_apply_port()`、`poll_serial()`、`send_ping()`；状态栏显示连接、MODE、LOCATION、本地时间、延迟等。 | 选择 COM5 连接，观察 PONG 延迟和状态栏。 | 满足。 |
| P2 | 控制面板：覆盖全部串口命令的可视化下发，提供参数组合下拉、缩写演示、大小写混合演示按钮。 | 当前“协议测试台”下拉框覆盖 PING/GET/SET DATE/TIME/ALARM/DISPLAY/FORMAT/MODE/MSG/BEEP/LED/WEATHER/RING/KEY 和错误测试；“缩写当前指令”“随机混合大小写”均作用于当前选中或手改的指令文本。本地/不使用串口模式也会按协议校验错误模板，返回 `ERROR LEN/SYNTAX/PARAM/RANGE (LOCAL)`，不会把错误指令误判为 `OK LOCAL`。 | 逐类选择模板，点击发送并查看日志 TX/RX；再对当前指令执行缩写和随机大小写后发送；不连串口时发送错误模板检查本地错误回包。 | 满足。 |
| P3 | 数字孪生镜像：软件仿真 8 位 7SEG + 8 LED + 8 位按键 + USER1/USER2；根据 `*EVT:DISP` 和 `*EVT:LED` 渲染，正确复现小数点；虚拟键下发 `*SET:KEY`；随 `*EVT:MODE` 同步。 | `pc_host/twin_widgets.py` + `handle_event()`；离线模式根据 `RuntimeState` 生成镜像；P1/P3 长文本页面会按跑马灯显示；虚拟按键支持约 0.8 秒长按，USER1 长按切 DAY/NIGHT，DISP 长按切显示开关。 | 连接真板看事件同步；离线点击/长按虚拟 DISP/FORMAT/USER1。 | 满足。 |
| P4 | 收发日志：时间戳、方向标记、颜色编码，可导出。 | 日志区显示 TX/RX/INFO/WARN/ERR，不同颜色；保留导出入口。 | 执行操作后查看日志；点击导出日志。 | 满足。 |
| P5 | 异常处理：串口占用、解析异常、网络超时、S800 ERROR 应答全部弹窗或日志高亮，GUI 不崩溃。 | 串口连接失败、NTP/天气失败、协议 ERROR 均写日志；自动测试给排查提示。 | 占用串口/输入错误命令/断网测试。 | 满足。 |

### 4.2 扩展功能 E1-E4

| 编号 | 老师原文要求 | 当前实现方式 | 演示方法 | 是否满足/备注 |
| --- | --- | --- | --- | --- |
| E1 网络对时 | 一键从 NTP 获取标准时间下发 S800；PC 监听 `*EVT:KEY USER1` 自动触发对时。 | NTP 多源和 HTTP Date 兜底；USER1 触发 `request_user1_time_sync()`；城市/国家输入会定位到城市或国家首都，按 IANA 时区和夏令时计算 offset，日志包含定位、时区、NTP 流程。 | 按板端 USER1 或点击一键对时，观察板端时间更新；输入 `Houston`、`Chicago`、`美国`、`日本` 等检查时区。 | 满足。 |
| E2 天气获取 | 自动获取本地天气，通过协议下发；USER2 数码管短显天气 5 秒，PC 镜像自动跟随。 | 天气多源兜底：Open-Meteo Forecast 为主，国内城市优先国内天气接口，失败时尝试 wttr；国家输入会转首都天气。`*SET:WEATHER DISP <token> LED <hex>`；USER2 短显缓存天气。 | 刷新天气后按 USER2，观察数码管/LED/镜像；输入中英文代表城市/国家检查日志流程。 | 满足。 |
| E3 自动昼夜模式 | PC 计算日出日落，自动下发 DAY/NIGHT；DAY 显示完整，NIGHT 只显示时分且 LED 仅保留心跳。 | `should_use_day_mode()`、`apply_auto_day_night()`；手动切换会关闭自动，仅写日志。 | 开启自动昼夜，切换城市/时间验证模式。 | 满足。 |
| E4 数据可视化看板 | 持久化记录闹钟/对时/事件等日志，绘制图表展示。 | 主页保留卡片式数据看板，并新增 Matplotlib 图表看板；图表从当前城市时间、天气缓存、板载闹钟、PC 多日程提醒和 `logs/events.jsonl` 中取数，支持“今日时间轴 / 提醒分布 / 系统状态 / 日志统计”四类筛选。系统状态图使用 `0/1`、`1/5`、`正常/开` 等实际状态文案，不用百分比。 | 在主页点击“Matplotlib 图表”，依次点“今日时间轴”“提醒分布”“系统状态”“日志统计”；切换 DAY/NIGHT 可看到图表主题同步变化。 | 满足；这是本轮补齐并加强的扩展功能 4。 |

### 4.3 自主增加功能

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 至少 1 项原创功能，不与 3/4.1/4.2 重复；简介 PDF 中写“动机-设计-实现-演示”并标注关键代码；演示视频完整呈现。 |
| 当前实现方式 | 多日程/课程提醒系统 + 铃声类型 + 语音空文本默认不启用；GitHub 版本管理；PC 页面风格与板端数码管显示联动的昼夜模式；全球时区/NTP/天气；快速/全面两套自动测试脚本；高并发和串口卡死保护；打包版 PC 程序提升可交付性。 |
| 实际效果 | 支持单次执行、每周重复、启停、铃声、板端短标签、PC 语音；触发后下发消息和铃声。 |
| 验收/演示方法 | 新增一次性提醒和每周提醒，观察表格状态、日志、板端消息和蜂鸣。 |
| 是否满足 | 满足；最终简介 PDF 和视频中必须明确讲“动机-设计-实现-演示”。 |

## 5. 串口通信协议

### 5.1 帧格式与容错规则

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 115200, 8N1，无流控；ASCII；行结束符支持 `\r`、`\n`、`\r\n`；单帧最大 64 字节；大小写不敏感，空格容错，缩写合法，错误命令必须回应 `ERROR <reason>`。 |
| 当前实现方式 | MCU 串口行缓冲、大小写/空格容错、缩写匹配；PC `protocol.py` 解析 OK/ERROR/EVT/PONG。 |
| 实际效果 | 支持常用缩写、随机混合大小写、多空格容错；错误进入日志和自动测试失败提示。 |
| 验收/演示方法 | 在“协议测试台”选择 `*SET:FORMAT RIGHT`，分别点击“缩写当前指令”“随机混合大小写”后发送；再发送错误参数模板。 |
| 是否满足 | 满足；参考文件建议 `ERROR LINE TOO LONG`，正式题目认可 `ERROR LEN`，当前用 `ERROR LEN`。 |

### 5.2 PC -> S800 命令总表

| 命令 | 老师原文要求 | 当前实现方式 | 是否满足 |
| --- | --- | --- | --- |
| `*RST` | 复位时钟/日期/闹钟，DISPLAY=ON，FORMAT=LEFT。 | `ResetRuntimeState()`。 | 满足。 |
| `*SET:DATE` | YEAR/MONTH/DATE 单参或组合。 | `HandleSetDate()`。 | 满足。 |
| `*SET:TIME` | HOUR/MINute/SECond 单参或组合。 | `HandleSetTime()`。 | 满足。 |
| `*SET:ALARM` | HOUR/MINute/SECond/OFF，设置后自动启用。 | `HandleSetAlarm()`。 | 满足。 |
| `*SET:DISPLAY` | ON/OFF 数码管亮灭。 | `HandleSetDisplay()`。 | 满足。 |
| `*SET:FORMAT` | LEFT/RIGHT。 | `HandleSetFormat()`。 | 满足。 |
| `*SET:MSG` | 不超过 32 字节滚动消息，保留大小写。 | `HandleSetMessage()` 保留原文。 | 满足。 |
| `*SET:BEEP` | 10-5000 ms 远程蜂鸣。 | `HandleSetBeep()`。 | 满足。 |
| `*SET:LED` | 2 位 HEX 直控 LED。 | `HandleSetLed()`。 | 满足。 |
| `*SET:KEY` | NAME 模拟按键，执行后不再上报 `*EVT:KEY`。 | `SimulateKeyPress()`，虚拟键走同一路径。 | 满足。 |
| `*SET:MODE` | DAY/NIGHT。 | `HandleSetMode()`。 | 满足。 |
| `*GET:*` | 查询 DATE/TIME/ALARM/DISPLAY/FORMAT。 | `HandleGet()`；另扩展支持 MODE。 | 满足。 |
| `*PING` | 应答 `*PONG <uptime_s>`。 | `UART_ProcessLine()`。 | 满足。 |

### 5.3 S800 -> PC 主动报文

| 报文 | 老师原文要求 | 当前实现方式 | 是否满足 |
| --- | --- | --- | --- |
| `*EVT:KEY <NAME>` | 物理按键触发，`*SET:KEY` 不上报避免环回。 | `EmitKeyEvent()`。 | 满足。 |
| `*EVT:ALARM` / `*EVT:ALARM_OFF` | 闹钟开始/停止。 | `StartAlarmRing()` / `StopBuzzer()` 路径。 | 满足。 |
| `*EVT:EDIT <TYPE> <VALUE>` | 本地编辑保存。 | `EmitEditEvent()`。 | 满足。 |
| `*EVT:DISP <8字符> <dpHex>` | 显示变化或 1 秒心跳；8 字符定长，`_` 填空，dpHex 表示小数点。 | `EmitDisplayEvent()`，1 Hz forceEvent。 | 满足。 |
| `*EVT:LED <hex>` | LED 变化或 1 秒心跳。 | `EmitLedEvent()`，1 Hz forceEvent。 | 满足。 |
| `*EVT:MODE <STATE>` | 模式切换。 | `EmitModeEvent()`。 | 满足。 |

### 5.4 FORMAT RIGHT 行为

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 例如当前时间 `12:30:45`，`*GET:TIME` LEFT 应答 `OK 12.30.45`，RIGHT 应答 `OK 54.03.21`；数码管同步逆序，小数点按下一位规则跟随。 |
| 当前实现方式 | `ReverseVisibleText()`、物理 dp 位渲染和 PC 离线镜像同规则。 |
| 实际效果 | `12.30.45` LEFT 上报 `12_30_45 24`，RIGHT 上报 `54_03_21 24`。 |
| 验收/演示方法 | 设置时间 12:30:45，切换 FORMAT，分别查询和观察镜像。 |
| 是否满足 | 满足。 |

### 5.5 完整会话示例

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 展示 `*PING`、`*SET:DATE`、`*SET:MSG`、`*GET:TIME`、`*SET:FORMAT RIGHT`、USER2 事件等完整会话。 |
| 当前实现方式 | 调试与测试页可发预设/原始命令；自动测试可逐项输出 OK/FAIL/SKIP。 |
| 验收/演示方法 | 运行自动测试，或手动复制会话命令观察日志。 |
| 是否满足 | 满足。 |

## 6. 评分标准与硬指标

| 项目 | 老师原文要求 | 当前状态 | 是否满足 |
| --- | --- | --- | --- |
| S800 板基础 | 52 分：显示稳定、按键稳定、开机画面、时钟、流水、闹钟、编辑、LED。 | 基础功能均有实现。 | 满足，需实板最终验证。 |
| 串口协议完整性 | 10 分：容错三件套、多参数组合、FORMAT 正确。 | 已支持大小写/空格/缩写，多参数组合和 RIGHT。 | 满足。 |
| PC 协同 | 14 分：控制面板、数字孪生、事件同步、日志。 | PC 2.0 已覆盖。 | 满足。 |
| 主观分 | 4 分：视频、文档、亮点。 | README 和本对照文档已补；最终 PDF/视频待做。 | 部分满足。 |
| 工程结构与提交规范 | 4 分：目录、命名、文档完整。 | 当前源码目录基本符合；`submission/` 已新增最终提交目录规划和简介 PDF/演示视频大纲。 | 基本满足，最终 zip 待整理。 |
| 扩展功能 | 8 分：E1/E2/E3/E4 分值。 | E1/E2/E3 完整，E4 已用 Matplotlib 图表看板补齐。 | 满足。 |
| 自主增加功能 | 8 分：原创功能，高分必选。 | 多日程提醒等。 | 满足。 |

硬指标自查：

- 显示稳定、无闪烁、亮度均匀：代码支持，需实板确认。
- 按键 1 秒内连续按 3 次以上不丢：防抖状态机支持，需实测。
- 编辑状态 5 秒无操作退出：已实现。
- 串口大小写/空格/缩写合法：已实现。
- `*SET:FORMAT RIGHT` 后应答与显示逆序，小数点正确：已实现。
- PC 数字孪生完整呈现 8 SEG + 8 LED + 8 按键 + USER1/USER2：已实现。
- 自编代码集中在 `main.c`：MCU 自编主逻辑集中在 `mcu/src/main.c`。

## 7. 提交要求

| 项目 | 老师原文要求 | 当前状态 | 是否满足/待办 |
| --- | --- | --- | --- |
| 工程目录结构 | 顶层含 `mcu/`、`pc_host/`、`docs/`、`README.md`；`mcu/src/main.c`；`pc_host/*.py/*.ui`；`requirements.txt`；可烧写 `.axf`。 | 当前目录具备源码、文档、README；`mcu/obj/s800_clock.axf` 已由最新源码重新编译；`submission/README.md` 已规划最终目录。 | 满足；最终只需按命名规则整理 PDF、视频和 zip。 |
| 命名规范 | 压缩包和简介文档按“学号-姓名”命名。 | 已按项目已有信息规划为 `大作业524031910102-陈云海.zip`、`大作业524031910102-陈云海.pdf`。 | 规划完成，最终打包时执行。 |
| 必交清单 | S800 与 PC 源码、`.ui`、`.axf`、requirements、简介 PDF、演示视频、README。 | 源码、`.ui`、requirements、README、`.axf` 已有；简介 PDF 与演示视频已有初稿大纲但未生成。 | 部分完成，PDF/视频待制作。 |

## 8. 阶段进度建议

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 第 1 周工程整合；第 2 周本地业务；第 3 周 PC 协同与提交。建议每周末打包一次工程。 |
| 当前实现方式 | 已进入提交准备状态：基础、扩展、PC、数字孪生、自动测试、README、打包版、提交目录说明、答辩初稿均已建立。 |
| 验收/演示方法 | 按 README 说明书和本对照文档逐项演示。 |
| 是否满足 | 进度满足；最终材料仍需制作。 |

## 9. 答辩说明

| 项目 | 对照内容 |
| --- | --- |
| 老师原文要求 | 答辩含抽查答辩和报名答辩；希望冲 A/A+ 的学生需自我报名；可能提问系统时基、I2C 显示扫描、按键防抖、编辑状态机、5 秒超时、串口容错、RIGHT 小数点、PC 数字孪生、双向同步、自主功能关键代码等。 |
| 当前准备方式 | 本文可作为要求对照；`docs/自主答辩准备初稿.md` 已整理答辩主线、关键代码、常见问题；关键代码位置：`mcu/src/main.c`、`pc_host/app.py`、`pc_host/twin_widgets.py`、`pc_host/extension_store.py`、`pc_host/extension_services.py`。 |
| 演示方法 | 现场按“开机 -> 本地按键 -> 串口连接 -> PC 控制 -> 数字孪生 -> NTP/天气 -> 日程提醒 -> 自动测试”的顺序。 |
| 是否满足 | 文档准备中；最终需结合简介 PDF 和视频。 |

## 补充参考文件自查

### `MCU与PC端开发要求_V1.0(1).pdf`

| 参考要求 | 当前处理 |
| --- | --- |
| SysTick 1 ms、主循环消费软节拍、主循环无阻塞 Delay。 | 已按 SysTick + tick 计数实现；需要 Keil/实板确认 1 秒误差。 |
| 长按阈值不低于 800 ms。 | 已按约 800 ms 口径处理。 |
| UART 行长 64 字节。 | 正式题目写 64 字节；当前错误返回 `ERROR LEN`，与正式题目错误集合一致。 |
| `*SET:MSG` 保留原大小写。 | 已保留。 |
| 滚动速度参考 500/250 ms。 | 正式题目只要求 2 级可调；当前使用较稳的两档节拍，消息和周几按 5-8 秒总时长收口。 |
| PC 推荐 Python 3.11.9。 | 当前源码可在现有 Python/PyQt5 环境运行；打包版不要求用户装 Python。最终若按老师环境演示，可准备 Python 3.11.9 说明。 |

### `FAQ_常见问题解析_V1.0(1).pdf`

| 参考要求 | 当前处理 |
| --- | --- |
| 建议开发目录用英文短路径，避免中文/空格。 | 当前项目路径含中文，已有打包时绕过中文路径的经验；最终提交前可按规范复制到短路径打包。 |
| venv 依赖隔离，requirements 明确。 | `pc_host/requirements.txt` 保留；打包版 `build_release/` 已被 `.gitignore` 排除。 |
| PyQt5/pyqt5-tools 版本注意。 | 当前无需强制安装 pyqt5-tools；源码运行依赖 PyQt5 + pyserial，打包版直接运行。 |
