# 当前项目状态总览

## 2026-06-17 补充：FAQ v1.3 最终收口状态

- PC 端、提交报告、release 包和 MCU 源码已按老师最新版 `FAQ_常见问题解析_V1.3(3).pdf` 对齐；`*EVT:DISP` 已改为 8 字符不含小数点 + `dpHex`。
- 老师新发 `testcmdV2.1` 原始 100 条命令实测：`OK 92`、`ERROR 8`、`TIMEOUT 0`，评分脚本给出 `10.0/10.0`。这里 8 个 `ERROR` 是测试集中故意检查非法命令拒绝能力的用例，不是项目扣分点。
- 自建严格回归脚本已加严到检查 FAQ v1.3 实板精确帧；2026-06-17 已 Keil5 重编、烧录 Verify OK，并复跑 COM5 到 `PASS=27 FAIL=0`。
- PC UI 已用真实 COM5 做读写验证：日期、时间、闹钟、DAY/NIGHT、PING 均能从界面下发并读回；截图保存在 `docs/testcmdV2.1新要求评估_2026-06-14/修复后复测/PC_UI_COM5_real_test.png`。
- 目前最重要的答辩口径：历史串口评分点已达满分且无 TIMEOUT；FAQ v1.3 后已重新生成并烧录最新 hex，COM5 严格回归已通过，最终只差录制演示 MP4。

## 2026-06-11 补充：NIGHT/FUNC 与流畅性

- NIGHT 模式普通时间页仍显示 `HH.MM`；进入 FUNC 时间编辑页时显示 `HH.MM.SS` 是正确行为，PC 端已经加保护，不再误触发夜间显示自纠。
- PC 的 DAY/NIGHT 切换、虚拟 USER1 长按等操作已改为“先返回，主题稍后合并刷新”，离屏探针显示模式切换调用约 1.5 ms，长按 USER1 约 12 ms，降低卡顿感。
- 全面自动测试新增 `FUNC TIME EDIT`，覆盖 FUNC 进入时间编辑、NIGHT 编辑态 `HH.MM.SS` 和测试后 MODE/FORMAT 恢复。
- MCU 主循环已加预算式调度，避免显示扫描/I2C/跑马灯积压长期阻塞 UART。该项已随 2026-06-17 最新固件重新 Keil5 编译、烧录，并纳入 COM5 严格回归。

更新时间：2026-06-11

主版本目录：`D:\桌面\大二下\大二下 嵌入式系统与接口技术\ARM\真正的最新版`

当前版本：`v2.2`

最新 Git 提交：本轮 v2.2 收口提交后以 `git log -1` 为准。

远端状态：本轮完成后会同步推送 `origin/main` 与 `origin/gemini-ui-improvements`。

## 1. 一句话概括

目前项目已经从“基础数字钟”扩展为一个可离线运行、可 PC 双向控制、带数字孪生、NTP/天气/自动昼夜、多日程提醒、Matplotlib 数据可视化和自动测试的智能联网时钟系统。历史 testcmdV2.1 真机评分已达 `10.0/10.0`；FAQ v1.3 后已完成 Keil5 重编重烧和 COM5 严格回归，最终只差录制演示 MP4。

## 2. 老师要求逐项状态

| 模块 | 当前状态 | 备注 |
| --- | --- | --- |
| S800 离线基础功能 | 基本完成 | 时钟、日期、周几、闹钟、编辑状态机、LED、蜂鸣、按键均有实现。 |
| 开机画面 | 完成 | 全亮/全灭、学号、拼音、`V2.2`。 |
| 数码管滚动 | 完成 | 支持 LEFT/RIGHT、两档速度、端点停顿、最后 2 秒停留、下划线七段码。 |
| 本地按键 | 完成 | FUNC/SHIFT/ADD/SAVE/DISP/SPEED/FORMAT/EXT/USER1/USER2 均有语义。 |
| 串口协议 | 完成 | `*SET`、`*GET`、`*EVT`、`*PING`、`ERROR <reason>`。 |
| PC 控制面板 | 完成 | 串口连接、时间写入、闹钟、显示、格式、模式、消息、蜂鸣、LED 等。 |
| 数字孪生 | 完成 | 8 位 7SEG、8 LED、SW1-SW8、USER1/USER2；串口连接时优先真实帧，未连接/不使用串口时即时本地模拟。 |
| 日志与异常 | 完成 | TX/RX/INFO/WARN/ERR；串口无响应 watchdog；NTP/天气 timeout。 |
| E1 网络对时 | 完成 | NTP/HTTP Date 兜底，连接/RESET/USER1 可触发。 |
| E2 天气获取 | 完成 | 天气 token + LED mask，下发板端，USER2 短显。 |
| E3 自动昼夜 | 完成 | 默认开启，城市/时区/日出日落判断，手动切换只写日志。 |
| E4 数据可视化 | 完成 | 主页支持“卡片看板 / Matplotlib 图表”切换，图表含今日时间轴、提醒分布、系统状态、24 小时日志统计四类筛选。 |
| 自主增加功能 | 完成 | 多日程/课程提醒系统，是冲自主答辩的重要亮点。 |
| 自动测试 | 完成 | 快速/全面两档，逐项 OK/FAIL/SKIP，覆盖协议和重要功能入口。 |
| 打包版 PC | 完成 | `build_release/SmartClockHost-v2.2.zip` 已生成，不提交 Git。 |
| 提交材料 | 准备中 | 已建立 `submission/` 大纲；简介 PDF、视频待制作。 |

## 3. 当前可演示功能清单

### MCU 端

- 开机动画：全亮、全灭、学号、拼音、版本。
- 时间走时：按秒更新。
- DISP：时间、日期、周几、年份循环。
- SPEED：滚动速度两档。
- FORMAT：LEFT/RIGHT 显示方向。
- FUNC/SHIFT/ADD/SAVE：日期、时间、闹钟编辑。
- 闹钟：到点蜂鸣，FUNC 停止，最多约 10 秒。
- LED：心跳、闹钟、编辑、串口收发、夜间模式、右向格式、扩展状态。
- USER1：
  - 短按：上报 PC，请求 NTP 对时。
  - 长按：板端 DAY/NIGHT 切换，并同步 PC。
- USER2：天气短显，有天气显示 token，无天气显示 `NO WX`。

### PC 端

- 串口连接：可选 COM 口，连接成功后自动排队 NTP。
- 数据看板：当前时间、日期星期、城市天气、昼夜模式、下次提醒、系统状态。
- Matplotlib 图表看板：今日时间轴、提醒分布、系统状态、24 小时日志统计四类筛选，支持白天/黑夜主题。
- 网络对时与天气：一键定位/天气/NTP/应用，后台执行。
- 数字孪生：真实串口帧优先，未连接时本地模拟。
- 日程管理：单次提醒、每周提醒、启停、铃声、板端标签、语音空文本规则。
- 调试与测试：
  - 协议模板覆盖全部主要指令和错误类型。
  - 支持缩写当前指令、随机混合大小写。
  - 不使用串口时，`*PING`、`*GET:*`、虚拟 DISP/FORMAT/MODE 等会立即给本地日志/影子镜像反馈，不等待板端。
  - 快速/全面联合测试。
- 异常恢复：
  - NTP 8 秒 watchdog。
  - 天气 14 秒 watchdog。
  - 串口 TX 后无 RX 时，自动清等待态，先 `EXT/PING`，再软 `RST`。

## 4. 当前关键文件

| 文件 | 作用 |
| --- | --- |
| `mcu/src/main.c` | MCU 全部核心业务逻辑。 |
| `mcu/clock.uvprojx` | Keil5 工程入口。 |
| `mcu/obj/s800_clock.axf` | 已提供最新可烧录文件；FAQ v1.3 后已用 Keil5 重编刷新并烧录 Verify OK。 |
| `pc_host/app.py` | PC 上位机主程序。 |
| `pc_host/twin_widgets.py` | 数字孪生绘制。 |
| `pc_host/protocol.py` | PC 端协议解析和命令构造。 |
| `pc_host/extension_services.py` | NTP、天气、语音等扩展服务。 |
| `pc_host/extension_store.py` | 配置、日程、运行态持久化。 |
| `pc_host/run_extension_checks.py` | 自动测试脚本。 |
| `README.md` | 使用说明书。 |
| `docs/大作业要求逐条验收对照.md` | 按老师要求逐条验收对照。 |
| `docs/test-guide.md` | 实板和 PC 联调测试步骤。 |
| `submission/README.md` | 最终提交目录和材料准备说明。 |

## 5. 已验证内容

最近一次复核已通过：

- Python 语法检查通过：`pc_host/app.py`、`protocol.py`、`twin_widgets.py`、`extension_services.py`、`extension_store.py`、`run_extension_checks.py`。
- host-only 全面测试通过，离线模式下 `*PING`、`*GET:*`、虚拟 DISP/USER2/滚动消息不会等待串口。
- 历史 COM5 quick 联板测试通过；FAQ v1.3 最新固件已烧录并通过 COM5 严格回归。
- 历史 COM5 full 联板测试通过，覆盖 PING、GET、SET DATE/TIME、MODE DAY/NIGHT、WEATHER、RING、USER2、跑马灯、DISP/SPEED/FORMAT/EXT、高并发按键和 ERROR 类型；FAQ v1.3 显示帧规则修改后已补充严格回归并通过。
- 真实 Qt UI 探针通过：约 `1080x639` 可用逻辑桌面下窗口 `1015x600`，分栏 `[569, 420]`，系统设置/闹钟与日程/调试与测试页横向滚动最大值为 0。
- Matplotlib 已改为按需加载；首次打开图表约 6.3 秒，后续四类筛选约 0.3-0.5 秒，图表错误为空。
- 本地模式即时响应探针通过：`DISP/USER2/MSG` 等操作约 26-49 ms 完成。
- release exe/zip 已重新生成；release 目录和 zip 无 `config.json/runtime_state.json/schedules.json/logs` 运行态残留，正式配置保存在 `%APPDATA%\SmartClockHost-v2.2`。
- release exe 临时 profile 冷启动未闪退；10 秒采样点出现一次短暂 `Responding=False`，15/25/35 秒恢复 `Responding=True`。这属于 PyInstaller/Qt 首次加载残余忙碌，后续操作和联板测试已通过。

## 6. 最终人工材料与可选复测

这些不是代码主功能缺口，而是最终交付和答辩前的人工准备：

1. 录制 5 分钟以内带旁白演示视频。
2. 生成 4-8 页简介 PDF，并把 testcmdV2.1 满分证据写进“测试与难点”页。
3. 按 `submission/README.md` 整理最终 zip。
4. Keil5 重编、烧录 Verify OK 和 `run_teacher_protocol_regression.py --port COM5` 严格回归 `PASS=27 FAIL=0` 已完成；最终提交前只需录制 MP4，并可选复跑老师 testcmdV2.1 确认仍为 `TIMEOUT 0`。
5. 正式演示当天提前打开打包版 exe，避免把 PyInstaller/Qt 首次加载时间录进视频。

## 7. 自主答辩策略

建议报名自主答辩。

理由：

- 项目已完成基础要求和 PC 协同。
- 扩展功能 E1/E2/E3/E4 均可演示，E4 现在使用 Matplotlib 图表看板。
- 自主增加功能“多日程/课程提醒系统”完整度较高，容易现场演示。
- 数字孪生、GitHub 版本管理、两套自动测试、串口恢复、高并发防卡死、全球时区、NTP/天气、PC/板端双向昼夜模式和舒适 UI 都是可讲的亮点。

答辩重点不要平均铺开，要抓三条线：

1. 基础功扎实：时基、显示、按键、闹钟、协议。
2. PC 协同完整：数字孪生、日志、双向同步、自动测试。
3. 亮点可解释：GitHub 版本管理、NTP/天气/全球时区、自动昼夜、多日程、Matplotlib 可视化、两套自动测试、稳定性保护和用户友好 UI。

## 8. 最后冲刺顺序

1. 生成简介 PDF。
2. 录制 5 分钟以内带旁白演示视频。
3. 把 testcmdV2.1 评分报告、PC UI 截图和关键实物视频片段收集到 `docs/` 或临时素材目录。
4. 按 `submission/README.md` 整理最终 zip。
5. 提交前再跑一次老师 testcmdV2.1 或自建严格回归，确认 `TIMEOUT 0`。
6. 打开打包版 PC 程序做一次白天/黑夜 UI 检查。
