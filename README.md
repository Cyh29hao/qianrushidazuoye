# 智能联网时钟系统

这个仓库是 ARM 大作业基础版工程，当前目标是先完成：
- `§3` S800 板本地基础功能
- `§4.1` PC 上位机核心功能
- `§5` 串口协议
- `§7` 提交目录结构

## 目录结构

```text
大作业524031910102-陈云海/
├─ mcu/
│  ├─ Inc/
│  ├─ Driverlib/
│  ├─ RTE/
│  ├─ src/main.c
│  ├─ obj/
│  ├─ listings/
│  ├─ clock.uvprojx
│  └─ clock.uvoptx
├─ pc_host/
│  ├─ .venv/
│  ├─ app.py
│  ├─ bootstrap_qt.py
│  ├─ diagnose_qt_runtime.py
│  ├─ launch_pc_host.ps1
│  ├─ launch_pc_host.bat
│  ├─ main.ui
│  ├─ ui_main.py
│  ├─ protocol.py
│  ├─ twin_widgets.py
│  └─ requirements.txt
├─ docs/
│  ├─ deployment.md
│  ├─ tech-notes.md
│  └─ debug-log.md
└─ README.md
```

## MCU 说明

- 基于官方 `EXP3` 模板改造。
- 自编核心逻辑集中在 [main.c](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/mcu/src/main.c>)。
- 当前已实现的主线包括：
  - `1 ms SysTick` 时基
  - 数码管动态扫描
  - 8 键 + USER1/USER2 防抖
  - 日期/时间/闹钟
  - 本地编辑状态机
  - `FORMAT LEFT/RIGHT`
  - 串口 `SET / GET / PING`
  - `*EVT:DISP / *EVT:LED / *EVT:MODE / *EVT:KEY / *EVT:ALARM / *EVT:EDIT`

## PC 说明

- 技术栈：`Python 3.12 + PyQt5 + pyserial`
- GUI 入口：`pc_host/app.py`
- 推荐启动入口：`pc_host/launch_pc_host.ps1`
- 当前上位机版本：`v2.1`
- Qt 运行时自检：`pc_host/diagnose_qt_runtime.py`

## 快速开始

### PC 上位机

详细步骤见 [deployment.md](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/docs/deployment.md>)。

最短路径：

```powershell
cd D:\桌面\大二下\大二下 嵌入式系统与接口技术\ARM\大作业524031910102-陈云海\pc_host
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple PyQt5 pyserial
python -m PyQt5.uic.pyuic -o ui_main.py main.ui
.\launch_pc_host.ps1
```

### MCU

- 打开 `mcu/clock.uvprojx`
- 目标芯片：`TM4C1294NCPDT`
- 自编代码位于 `mcu/src/main.c`

## 当前状态

- Python 侧已完成：
  - `PyQt5` 与 `pyserial` 安装
  - `ui_main.py` 生成
  - 模块导入检查
  - `pyuic5` 可执行检查
- MCU 侧已完成：
  - `main.c` 主逻辑落地
  - `gcc -fsyntax-only` 语法检查
- 尚未完成：
  - 真机烧录验证
  - 实串口联调
  - `§4.2` 扩展功能
  - `§4.3` 自主增加功能

## 文档

- 部署与排错： [deployment.md](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/docs/deployment.md>)
- 联调与验收： [test-guide.md](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/docs/test-guide.md>)
- 答辩技术点： [tech-notes.md](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/docs/tech-notes.md>)
- 调试记录： [debug-log.md](</D:/桌面/大二下/大二下 嵌入式系统与接口技术/ARM/大作业524031910102-陈云海/docs/debug-log.md>)

## 说明书 README 部分

### 使用目录

- [1. 程序组成](#1-程序组成)
- [2. MCU 端启动](#2-mcu-端启动)
- [3. PC 端启动](#3-pc-端启动)
- [4. 串口连接步骤](#4-串口连接步骤)
- [5. 主界面与数据看板](#5-主界面与数据看板)
- [6. 网络对时与天气](#6-网络对时与天气)
- [7. 昼夜模式与自动切换](#7-昼夜模式与自动切换)
- [8. 数字孪生镜像](#8-数字孪生镜像)
- [9. 蜂鸣、闹钟与单次闹钟](#9-蜂鸣闹钟与单次闹钟)
- [10. 日程管理](#10-日程管理)
- [11. 滚动消息](#11-滚动消息)
- [12. 系统设置与用户名](#12-系统设置与用户名)
- [13. 自动测试](#13-自动测试)
- [14. 常见问题排查](#14-常见问题排查)

### 1. 程序组成

本项目由 MCU 端和 PC 上位机两部分组成。MCU 端运行在 S800/TM4C1294 板卡上，负责数码管、LED、按键、蜂鸣器、闹钟和串口协议。PC 端是 PyQt5 上位机，负责串口控制、网络对时、天气获取、数字孪生镜像、日程和自动测试。

### 2. MCU 端启动

1. 用 Keil5 打开 `mcu/clock.uvprojx`。
2. 确认目标芯片为 `TM4C1294NCPDT`，连接 S800 板卡调试器。
3. 编译并下载程序到板卡。
4. 按板卡 `RESET` 后，数码管会进入开机画面，然后进入正常时钟显示。

本轮已修改 MCU 时钟源配置和滚动消息节拍，重新体验前需要重新烧录 MCU。

### 3. PC 端启动

源码版启动：

```powershell
cd D:\桌面\大二下\大二下 嵌入式系统与接口技术\ARM\真正的最新版\pc_host
.\launch_pc_host.ps1
```

如果第一次运行源码版，先安装依赖：

```powershell
cd D:\桌面\大二下\大二下 嵌入式系统与接口技术\ARM\真正的最新版\pc_host
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

打包版启动：

```text
build_release/SmartClockHost-v2.1/SmartClockHost.exe
```

打包版为 PyInstaller onedir 形式，普通用户双击 `SmartClockHost.exe` 即可启动，不需要额外安装 Python。若同时提供压缩包，文件名通常为 `build_release/SmartClockHost-v2.1.zip`。

### 4. 串口连接步骤

1. 先把 S800 板卡通过 USB 连接到电脑，并确认板端程序已运行。
2. 打开 PC 上位机，在串口下拉框中选择扫描到的端口，例如 `COM5`；如果下拉框暂时没有显示，也可以直接输入 `COM5`。
3. 点击连接按钮。连接成功后，状态栏会显示已连接端口，日志会出现连接成功和板端响应信息。
4. 点击查询或等待心跳事件，数字孪生镜像应开始跟随板端数码管和 LED。

如果只想离线演示 PC 端功能，可以选择“不使用串口”。此时数字孪生镜像会显示一个本地模拟板卡状态，仍可体验对时、天气、昼夜、消息和日程等功能。

### 5. 主界面与数据看板

串口连接状态放在页面上方“串口连接”模块；主页数据看板只保留适合展示的信息，包括当前时间、日期星期、城市天气、昼夜模式、下次提醒、系统状态和问候语。问候语会按时段显示“早上好 / 中午好 / 下午好 / 晚上好 / 夜深了”，用户名可在系统设置中修改。

日期和时间编辑框用于设置板端时间。修改后点击对应设置按钮会通过串口发送到 MCU；离线模式下会修改 PC 本地镜像时间。

### 6. 网络对时与天气

“网络对时与天气”模块用于选择城市、NTP 对时、刷新天气并把结果应用到板端。

使用步骤：

1. 在城市输入框中输入城市名，例如 `上海`、`北京`、`New York`。
2. 点击“一键对时、刷新天气并应用”。日志会立即提示已开始更新。
3. 上位机会保存当前城市，定位经纬度和时区，执行 NTP 对时，刷新天气，并按天气结果更新板端短显和 LED。
4. 当前时间显示会每秒按所选城市时区刷新；国外城市会按对应 IANA 时区和夏令时规则计算。

NTP 成功后，PC 会以获取到的城市时间作为基准，再用单调时钟本地推进，避免重复加秒或 UI 定时器造成走时偏快。

串口连接成功、板端硬件 RESET/启动、协议台发送 `*RST` 后，PC 会自动排队执行一次 NTP 对时并写入 S800；若 NTP 超时或失败，会改用当前城市/PC 时间作为兜底写入，日志会说明来源。手动发送 `*SET:TIME` 或 `*SET:DATE` 只写入时间，不会误触发 NTP。写入板端时会先等 `SET DATE` 返回 `OK` 再发送 `SET TIME`，避免命令过密导致日期写入偶发报错。

如果串口 TX 后数秒内没有任何 RX，上位机会自动清理未完成等待态，先尝试发送 `EXT/PING` 让板端退出临时显示；仍无响应时会尝试软 `RST` 并在日志提示。`USER1` 连续短按/长按会被合并节流，避免重复 NTP 或昼夜模式切换把串口状态机压住。

### 7. 昼夜模式与自动切换

白天/黑夜模式可手动切换，也可启用自动昼夜切换。自动模式在 v2.1 安装包默认开启，会根据当前城市时间、日出日落信息或默认时间段判断应使用 DAY 还是 NIGHT。

注意：PC 端按钮和板端 `USER1` 长按都可以切换昼夜模式，并会同步到另一端。用户手动切换另一昼夜模式时，上位机会关闭自动切换，并只在日志记录原因。切换城市或执行一键对时/天气刷新不会主动清空自动昼夜勾选状态。RESET/重连后的短时间内以 PC 当前模式为准，PC 会把 DAY/NIGHT 同步回板端，避免板端默认状态污染上位机显示。

### 8. 数字孪生镜像

数字孪生镜像用于在 PC 端同步显示板端数码管、LED 和按键状态。

- 串口已连接时，以 MCU 上报的 `*EVT:DISP`、`*EVT:LED`、`*EVT:KEY` 为准。
- 串口未连接时，以 PC 本地运行时状态生成镜像，方便离线演示。
- 板端开机显示 `88888888`、学号后八位、`V2.1` 版本/提示语时，PC 端会尽量同步显示；如果 PC 较晚连接，后续收到真实显示事件后会自动进入当前状态。
- `*EVT:DISP` 中 `_` 仍表示空白物理位；如果滚动消息里需要真实显示下划线，MCU 会用 `~` 上报，PC 数字孪生会还原为七段码底段。
- `USER2` 在非编辑状态下用于天气短显：有缓存天气时显示天气 token，没有天气时显示 `NO WX`，PC 日志会记录触发来源和显示内容。

### 9. 蜂鸣、闹钟与单次闹钟

蜂鸣测试和 LED 掩码设置位于“调试与测试”页面的板端硬件测试区域；日志会记录发送的命令和板端反馈。

普通闹钟使用时间编辑框设置时分秒，可启用或关闭。到点后板端蜂鸣器响铃，PC 日志和状态区会显示闹钟事件。

单次闹钟模块会直接显示“当前状态：开/关    触发时间：xxxx”。一行按钮中保留“查询”和“启用/关闭”：关闭时按钮显示“启用”，开启时按钮显示“关闭”。查询按钮用于重新读取板端状态。

### 10. 日程管理

日程管理用于添加课程、会议、提醒等事项。新增日程默认启用提醒。日程项是否启用只通过双击表格项切换。

铃声预览入口放在具体模块内：闹钟/日程的铃声选择框旁边有“预览”按钮。语音播报也按模块控制，闹钟和日程分别有自己的“启用语音播报”选项；语音文本为空时默认不播报。即使语音不可用，普通日志和蜂鸣提醒仍可继续使用。

### 11. 滚动消息

滚动消息用于把较长文本显示到 8 位数码管。输入文本后点击发送，MCU 和 PC 数字孪生会尽量保持同样的滚动过程。

当前逻辑会让消息至少显示 5 秒，最长不超过约 10 秒；到达最左端或最右端会多停一个节拍，滚动文本至少会往返碰到一次端点，最后一帧额外停留约 2 秒再恢复时钟。短文本不需要滚动时，也会至少维持显示 5 秒。

### 12. 系统设置与用户名

系统设置中保留全局显示、FORMAT、DAY/NIGHT、滚动消息、城市/时区、自动昼夜和用户名等设置。蜂鸣、LED 掩码、协议测试等偏调试功能在“调试与测试”页面；语音播报入口在闹钟和日程各自模块内；自动测试只由用户手动点击运行。

用户名默认是“用户”。输入新的用户名后点击确认，主页问候语会立即更新，并持久化保存到配置文件，下次打开 PC 端仍会使用该名称。

### 13. 自动测试

自动测试界面用于检查串口协议、显示、LED、模式、闹钟、天气短显、数字孪生和扩展功能，只会在用户手动点击测试按钮后启动，不会开机自动运行。

界面提供两个入口：`快速联合测试` 覆盖 PING、GET、日期时间写入、昼夜切到另一模式再切回、天气短显、铃声和 USER2；`全面联合测试` 会额外覆盖跑马灯下划线、DISP/SPEED/FORMAT/EXT 按键、全部 ERROR 类型，并在串口项通过后异步触发一次城市/天气/NTP 一键流程。测试会保留自动昼夜开关的原状态，不会因为模式测试把自动昼夜关掉。

点击开始测试后，界面会显示预计测试时间。每完成一个测试点，会追加一行 `OK`、`FAIL` 或 `SKIP`。测试全部结束后，失败项会附带简短排查方向，便于定位是串口、板端响应、显示同步还是网络条件问题。

“调试与测试”页还提供“协议测试台”：下拉框包含各类完整命令模板；`缩写当前指令` 和 `随机混合大小写` 都作用于当前文本框里的指令，最后统一用 `发送当前指令` 下发并在日志查看 `TX/RX`。错误模板覆盖 `ERROR LEN`、`ERROR SYNTAX`、`ERROR PARAM`、`ERROR RANGE`，用于演示异常格式符合 `ERROR <reason>`。

### 14. 常见问题排查

串口连不上：

- 确认板卡 USB 已连接，Keil 串口调试没有占用同一个端口。
- 点击刷新串口，或在下拉框中手动输入 `COM5` 后连接。
- 连接成功后应能看到日志、状态栏端口和板端 `READY`/心跳事件。

天气刷新失败：

- 先看日志中的网络错误和城市名是否正确。
- 不开代理时优先保证 NTP；天气接口可能受网络环境影响，可换国内城市或稍后重试。
- 开代理后如果 NTP 异常，确认代理没有拦截 UDP/HTTP 时间请求；本项目会尽量尝试多个 NTP 源。

时间不对或走时偏快：

- 先执行“一键对时、刷新天气并应用”。
- 国外城市请检查日志中的城市、时区和 UTC offset，例如纽约夏令时应显示 `UTC-04:00`，冬令时应显示 `UTC-05:00`。
- 如果板端每分钟仍明显偏快，需要确认已经重新烧录本轮 MCU 程序。

闹钟不响：

- 检查闹钟是否启用，触发时间是否是板端当前时间之后。
- 检查是否处于夜间静音或蜂鸣器测试失败。
- 查看日志中是否出现 `*EVT:ALARM` 事件。

数字孪生不同步：

- 串口连接时以 MCU 上报为准，先确认日志中有 `*EVT:DISP` 和 `*EVT:LED`。
- 如果刚连接时还停留在旧画面，等待下一次心跳或点击查询运行状态。
- 离线模式下数字孪生显示的是 PC 本地模拟状态，不代表真实板卡。
- 如果滚动消息包含 `_`，应显示为七段码底段；日志中的 `*EVT:DISP` 可能会看到 `~`，这是为了区分真实下划线和空白位。
