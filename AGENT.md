# AGENT 开发守则

## 串口、RESET 与数字孪生同步硬规则

- 串口已连接时，数字孪生的数据源优先级必须是：板端真实 `*EVT:DISP`/`*EVT:LED` 帧优先；短时间没有新帧时保留最后一帧或等待状态；只有未连接串口/本地模式才允许 PC 本地模拟画面。
- 网络对时、天气刷新、开机镜像、本地预览、自动测试提示都不能覆盖连接状态下的真实板端显示。PC 可以发指令，但显示结果应以板端回传为准。
- RESET、断电重插、串口重连时，要先清理旧查询队列，再做写时、查询、天气等后台任务，避免 `SET` 的 `OK` 被误当作 `GET:MODE`、`GET:DISPLAY` 等返回。
- `MODE` 展示只能是 `DAY` 或 `NIGHT`；自动昼夜开关的 `ON/OFF`、显示开关的 `ON/OFF` 和内部禁用状态不能污染昼夜模式 UI。
- 板端离线恢复时间只能使用稳定封装：优先 RTC/EEPROM 等明确可用能力；不要为了“恢复时间”直接写危险 Flash 逻辑。没有 RTC 时必须说明只能恢复最近保存时间，无法估计断电时长。

本文件约束后续接手本仓库的 Codex、Gemini CLI 或其他 AI 编程代理。目标是保护当前智能时钟联网系统 demo 的稳定性，尤其避免反复把 PC 端 UI 暗色主题、按钮、布局和说明文档改坏。

## 接手和修改边界

- 当前主版本是本目录 `真正的最新版`，不要回到父目录里的旧“最新版”继续开发，也不要用旧目录覆盖当前工程。
- 开始任何修改前，先读 `PROJECT_CONTEXT.md`、`AGENTS.md`、本文件和当前代码，再看旧交接文档。
- 优先做最小稳定修改，不要大改架构，不要重新设计一个完全不同的 UI。
- 不要改变 MCU 通信协议，除非用户明确要求且已经说明协议影响。
- 不要破坏现有串口连接、数字孪生镜像、闹钟、日程、天气、滚动消息、无串口本地模式等核心结构。
- 不要引入复杂新依赖；新功能以提升演示体验为目标。

## UI 主题硬规则

- 修改 UI 前必须同时考虑白天模式和黑夜模式，不能只看一个主题。
- 黑夜模式禁止出现突兀白底、浅色块、黑字不可见、灰字过暗、浅底白字等问题。
- 白天模式也不能因为修黑夜模式而变得对比不足或布局散乱。
- 按钮必须保持统一蓝色主按钮风格，不能变成无色、灰白、像不可点击控件。
- 按钮需要同时有清楚的默认、悬停、按下、禁用状态。
- 下拉框、滚动条、页脚、状态栏、输入框、表格、日志区、日历弹窗、下拉弹出列表都必须有明确的深色主题规则。
- 状态栏/页脚中的 QLabel 要直接套主题色，避免被系统默认调色板变成深蓝字或黑字。
- 局部 `setStyleSheet()` 不能只写背景或边框而遗漏文字色；如果给控件写局部样式，必须同时覆盖暗色主题所需的前景色、背景色、边框和选中态。
- 不能让父级 QSS 或局部 QSS 覆盖掉全局暗色主题；必要时在 `_apply_dynamic_theme_overrides()` 里统一补齐。
- Qt 下拉箭头、spinbox 箭头和 checkbox 勾选标记不要再用 QSS border 三角形硬画；在 Windows 缩放下容易变成黑色小方块。优先使用仓库内稳定的 XPM/位图资产，并同时检查白天/黑夜可见性。

## 布局和展示规则

- 主窗口必须保持稳定左右分栏：左侧主内容不能用过大的最小宽度撑爆右侧数字孪生，右侧数字孪生和日志区不能覆盖左侧页面；在 1280x720、1366x768 和全屏下都要检查。
- 左侧页面使用滚动时，页面 host 宽度不得超过 scroll viewport 宽度；系统设置、自动测试、网络状态等长文本必须可换行/可收缩，不能把按钮或文本框挤到右边界外。
- 右侧数字孪生必须优先完整显示 7SEG、LED 和两行按键；日志区只能占用剩余空间，不能把第二行按键裁掉或压住。
- 所有分组标题、小标题不能压线或碰到边框；要么完整放到框内，要么完整放到框外。
- 控件之间不能重叠，普通窗口和全屏/宽屏模式都要检查。
- 数据看板保持卡片式、清楚、大气，不能退回一行行堆文本。
- 串口状态应放在“串口连接”模块，不要塞回数据看板。
- OTA 不要再出现在界面和最终说明文档中；历史聊天归档可以保留为历史材料，但不能作为当前功能露出。
- 页面结构保持当前四块：主页、系统设置、闹钟与日程管理、调试与测试。

## 验证要求

- UI 改动后必须检查主要页面的白天/黑夜两套显示效果：主页、系统设置、闹钟与日程管理、调试与测试。
- 至少做一次普通窗口检查；涉及布局宽度时再做宽屏/全屏检查。
- 用程序化检查确认可见按钮仍有蓝色主按钮样式，确认没有可见 OTA 控件。
- 运行 Python 语法检查，至少覆盖 `pc_host/app.py` 和相关 PC 端模块。
- 能跑 host-only 自动测试时要运行，确认本地模式、协议影子状态和扩展逻辑没有被 UI 修改带坏。
- 自动测试不能在 NTP 对时、串口时间写入或天气刷新仍在进行时抢占同一个串口；需要等待这些流程空闲，或设置明确 timeout/FAIL 结果。
- 截图验证时注意：Qt offscreen 平台可能不渲染中文或部分文字，最终视觉判断优先使用 Windows 原生 Qt 平台截图或真实窗口。

## Git 和文档

- 不要提交 `.venv/`、`__pycache__/`、`tmp/`、`build_release/`、日志、运行态缓存、Keil 中间产物。
- 每次用户同意上传 GitHub 时，更新 `PROJECT_CONTEXT.md` 和 `CHANGELOG_AI.md`，只记录最终状态、关键文件、验证结果和未解决问题。
- README 和验收对照文档只写当前真实功能，不要重新加入已删除的 OTA 或旧占位说明。
